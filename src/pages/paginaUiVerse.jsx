/* import {} from 'react'

export default (props) => {

return (
    <div>
        <h1>Oi!!</h1>
            <button className={style}> 
                Botao 1
            </button>
    </div>
)
}

const style = {
    button {
        glowColor = rgb(217, 176, 255),
        glowSpreadColor =  rgba(191, 123, 255, 0.781),
        enhancedGlowColor = rgb(231, 206, 255),
        btnColor = rgb(100, 61, 136),
        border = 25em, solid, var(glowColor),
        padding =  1em, 3em,
        color: var(glowColor),
        fontSize = 15px,
        fontWeight = bold,
        backgroundColor = var(btnColor),
        border-radius = 1em,
        outline = none,
        boxShadow= 0 0 1em .25em var(glowColor),
               0 0 4em 1em var(glowSpreadColor),
               inset 0 0 .75em .25em var(glowColor),
        textShadow= 0 0 .5em var(glowColor),
        position= relative,
        transition= all, 0.3s,
       }
       
       button::after {
        pointerEvents =  none
        content = ""
        position = absolute
        top = 120%
        left = 0
        height = 100%
        width =100%
        backgroundColor = var(glowSpreadColor)
        filter =  blur(2em)
        opacity= .7
        transform = perspective(1.5em) rotateX(35deg) scale(1, .6);
       }
       
       button:hover {
        color = var(btnColor);
        backgroundColor= var(glowColor);
        boxShadow= 0 0 1em .25em var(glowColor),
               0 0 4em 2em var(glowSpreadColor),
               inset 0 0 .75em .25em var(glowColor);
       }
       
       button:active {
        boxShadow= 0 0 0.6em .25em var(glowColor),
               0 0 2.5em 2em var(glowSpreadColor),
               inset 0 0 .5em .25em var(glowColor);
       }
}
 */

